<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.github.zyro.crunchbase"
          android:versionCode="1"
          android:versionName="1.0.0">

    <uses-sdk android:minSdkVersion="15" android:targetSdkVersion="15"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application android:label="@string/app_name"
                 android:icon="@drawable/launcher"
                 android:theme="@style/Theme.CrunchBase">
        <activity android:name=".activity.SplashActivity_"
                  android:label="@string/app_name"
                  android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.HomeActivity_"
                  android:label="@string/app_name"
                  android:configChanges="orientation|screenSize"/>
        <activity android:name=".activity.CompanyActivity_"
                  android:label="@string/app_name"
                  android:configChanges="orientation|screenSize"/>
    </application>

</manifest>